{%- assign section_color = section.settings.color_scheme -%}
{%- include 'global-variables' -%}

{%- assign tags_enabled = section.settings.enable_tag_filter -%}
{%- assign sorting_enabled = section.settings.enable_sort_filter -%}
{%- assign posts_per_row_mobile = section.settings.posts_per_row_mobile -%}
{%- assign posts_per_row_desktop = section.settings.posts_per_row_desktop -%}

<section class="collection color--{{ section_color }}-text" data-section-type="collection" data-section-id="{{ section.id }}" data-fade-in="{{ fade_in }}">
    <div class="max-site-width md--up--px2">
      {% include 'collection__header' %}
    </div>

    <div class="max-site-width md--up--px2 bg--secondary-background" id="collection-top">
      <div class="page-section lg--up--ten-twelfths mx--auto px2 md--up--px0">
        {% include 'collection__list-products' %}
      </div>
    </div>
  </div>
</section>
